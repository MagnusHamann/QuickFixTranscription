# Local Whisper Models

Place local Whisper model files here if you want QuickFixTranscription to auto-detect them.

Supported extensions:

- `.bin`
- `.gguf`

Do not commit model files to git. They are large binary dependencies and should be installed or selected locally.
